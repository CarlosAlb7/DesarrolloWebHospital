@model PruebaGit.Web.Models.Hospital

@{
    ViewBag.Title = "Delete";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card" style="background-color:aliceblue; color:blue;">
                <div class="card-header">
                    <h3 class="card-title text-primary">Estas seguro que quieres eliminar este registro?</h3>
                    <div>
                        <hr />

                        <dl class="dl-horizontal">
                            <dt>
                                @Html.DisplayNameFor(model => model.Numero_Hospital)
                            </dt>

                            <dd>
                                @Html.DisplayFor(model => model.Numero_Hospital)
                            </dd>

                        </dl>
                        
                        @using (Html.BeginForm())
                        {
                            @Html.AntiForgeryToken()

                    <div class="form-actions text-center">
                        <input type="submit" value="Eliminar" class="btn btn-danger" />
                           <a href="@Url.Action("Index","Hospitals")" class="btn btn-primary"> Regresar </a> 
                       
                    </div>  
                        }
                    </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
